<network>
  <name>{{ .Name }}</name>
  <bridge name="virbr0"/>
  {{ if .Internal != true }}<forward mode="nat"/>{{ end }}
  <ip address="{{ .Address }}" netmask="{{ .Mask }}">
  {{ if .DHCP }}
    <dhcp>
      <range start="{{ .DHCP.Start }}" end="{{ .DHCP.End }}"/>
      {{ range .DHCP.Hosts }}
        <host mac="{{ .MAC }}" ip="{{ .IP }}" />
      {{ end }}
    </dhcp>
  {{ end }}
  </ip>
</network>